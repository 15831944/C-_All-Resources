杨军
13350873960 yanghuajun@163.com

18*4
老谭 编程的基本过程 C的语法 常用的算法
vc6.0 
程序的结构 顺序 分支 循环
变量 数据的类型 
运算符 
算术 + - * / % 
关系 > < >= <= == !=
逻辑 && || !   1&&5
位运算符 & | ~ ^
定义和使用数组的语法 一维 二维
定义一个存放班级成绩一维数组，找出最高的成绩及其位置。
函数的定义和调用，以及函数的参数传递
找最大值的功能写到一个函数里。
递归函数。难
指针 难
定义指针 指针的初始化 通过指针访问其指向的变量
int * p;  int a; p = &a;  *p
p = &a[0];  <==>  p = a;  p++;
p[i] == *(p+i)
数组名本质上就是数组首元素的地址。
字符数组来表示字符串 数组
结构体，可以定义新的类型。结构体类型可以用来模拟
需要的数据类型，在语法上就是将多个数据类型的变量
集成在一起。
定义结构体类型的语法。定义和使用结构体类型的变量。
结构体类型的数组。如何在定义结构体变量的同时对其进行初始化。
怎样通过输入对结构体变量进行赋值。
定义一个表示学生信息的结构体类型，学号，姓名，数学，语文
英语的成绩。

定义一个学生结构体数组，完成数据的赋值（初始化或者输入），
找出总分最高的学生的总分及其信息。

18*4 72
面向对象程序设计 C++
